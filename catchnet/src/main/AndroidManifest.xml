<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wanggle.catchnet">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatchNet">

        <provider
            android:authorities="com.wanggle.catchnet.provider.CustomContentProvider"
            android:name=".provider.CustomContentProvider"
            android:exported="true"/>

        <activity android:name=".view.activity.CatchNetActivity" />
        <activity android:name=".view.activity.CatchNetDetailActivity" />

    </application>

</manifest>